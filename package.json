{
  "name": "dynamic-report-tool",
  "version": "1.0.0",
  "scripts": {
    "client": "ng serve --prefix dynamic-report-app",
    "server": "node index.js --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
